package epam.stepdefinitions;\n\nimport epam.api.PetApi;\nimport epam.dto.Pet;\nimport epam.dto.Category;\nimport epam.dto.Tag;\nimport io.cucumber.java.en.Given;\nimport io.cucumber.java.en.Then;\nimport io.cucumber.java.en.When;\nimport io.restassured.response.Response;\nimport org.junit.Assert;\n\nimport java.util.Arrays;\n\npublic class PetStepDefinitions {\n\n    private PetApi petApi;\n    private Pet pet;\n    private Response response;\n\n    public PetStepDefinitions() {\n        petApi = new PetApi();\n    }\n\n    @Given(\"a pet with id {long}, name {string}, category {string}, status {string}\")\n    public void a_pet_with_id_name_category_status(Long id, String name, String category, String status) {\n        Category cat = new Category(id, category);\n        pet = new Pet(id, cat, name, Arrays.asList(\"url1\", \"url2\"), Arrays.asList(new Tag(id, \"tag1\")), status);\n    }\n\n